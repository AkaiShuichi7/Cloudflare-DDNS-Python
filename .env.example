# =================================================================
# cfstddns - 自动更新 Cloudflare DNS 的 Python 实现
#
# 说明:
# 1. 将此文件重命名为 .env
# 2. 根据下面的注释填写所有必要的配置项
# =================================================================

# --- 任务调度配置 ---
# Cron 表达式，用于定义定时执行任务的时间。
# 格式: 分 时 日 月 周 (例如 "0 3 * * *" 表示每天凌晨3点执行)
# 如果留空，程序将不会设置定时任务。
CRON_SCHEDULE="0 3 * * *"

# 是否在程序启动时立即执行一次任务。
# 可选值: true / false
RUN_ON_STARTUP="true"


# --- Cloudflare API 配置 ---
# 访问 https://dash.cloudflare.com/profile/api-tokens 获取
# 必须提供 "Global API Key"
CF_API_KEY=""

# 你的 Cloudflare 登录邮箱
CF_EMAIL=""

# 你的域名所在的区域 ID (Zone ID)
# 在 Cloudflare 仪表盘域名概览页的右下角可以找到
CF_ZONE_ID=""

# --- DDNS 配置 ---
# 需要进行动态 DNS 更新的域名列表，多个域名请用空格隔开。
# 例如: "cdn.example.com cdn2.example.com"
CF_HOSTNAMES=""


# --- CloudflareST 测速工具配置 ---
# 工作模式，选择测速的 IP 类型。
# 可选值: ipv4 / ipv6
IP_VERSION="ipv4"

# 自定义测速地址，用于 CloudflareST 进行 HTTP 测速。
# 建议使用自建的、稳定的网站地址。如果留空，将使用 CloudflareST 的默认地址。
CFST_URL=""

# 测速端口，CloudflareST 进行延迟和下载测速时使用的端口。
# 默认: 443
CFST_PORT="443"

# 下载测速超时时间（秒），单个 IP 的最大下载测速时长。
# 默认: 10
CFST_DOWNLOAD_TIMEOUT="10"

# 测速线程数量，根据设备性能调整。
# 性能较弱的设备（如路由器）建议不要设置太高。
# 默认: 200
CFST_THREADS="200"

# 延迟测速次数，对单个 IP 进行延迟测试的次数。
# 默认: 4
CFST_TEST_COUNT="4"

# 下载测速数量，在延迟排序后，选择前 N 个 IP 进行下载速度测试。
# 默认: 10
CFST_DOWNLOAD_COUNT="10"

# 平均延迟上限（毫秒），只选择平均延迟低于此值的 IP。
# 默认: 9999 (不过滤)
CFST_MAX_LATENCY="300"

# 平均延迟下限（毫秒），只选择平均延迟高于此值的 IP。
# 默认: 0 (不过滤)
CFST_MIN_LATENCY="40"

# 丢包率上限，只选择丢包率低于或等于此值的 IP (0.00 到 1.00)。
# 0 表示完全不允许丢包。
# 默认: 1.00 (不过滤)
CFST_MAX_PACKET_LOSS="0.2"

# 下载速度下限（MB/s），只选择下载速度高于此值的 IP。
# 默认: 0.00 (不过滤)
CFST_MIN_DOWNLOAD_SPEED="10"
